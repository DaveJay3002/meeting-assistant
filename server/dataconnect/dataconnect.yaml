specVersion: "v1"
serviceId: "server"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "server-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
